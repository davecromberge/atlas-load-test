simulation {
    # Url to atlas host:
    host = "http://127.0.0.1:7101"

    # Duration in hours
    duration = 10

    # tags endpoint
    tags.api = "/api/v1/tags"

    # publish endpoint
    publish.api = "/api/v1/publish"

    # fetch endpoint
    fetch.api = "/api/v2/fetch"
}
